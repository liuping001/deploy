deploy_info:
  server1:
    push:
      - src: example/test.py
        dest: /tmp/server_1.py
      - src: example/supervisor.conf
        dest: /tmp/supervisor.conf
    init_cmd:
      - "mkdir -p /tmp/server_1_log"
    supervisor_conf: /home/liuping/super/supervisord.conf

  server2:
    push:
      - src: example/test.py
        dest: /tmp/server_2.py
      - src: example/supervisor.conf
        dest: /tmp/supervisor.conf
    init_cmd:
      - "mkdir -p /tmp/server_2_log"
    supervisor_conf: /home/liuping/super/supervisord.conf

